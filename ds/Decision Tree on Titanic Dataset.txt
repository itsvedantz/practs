---Decision Tree on Titanic Dataset---
install.packages("rpart")
library(rpart)
library(rpart.plot)

data(Titanic)

titanic_data <- as.data.frame(Titanic)

model <- rpart(Survived ~ Class + Sex + Age, data=titanic_data, method="class")

print(model)

rpart.plot(model)


height <- c(151,174,138,186,128,136,179,163,152)
weight <- c(63,81,56,91,47,57,76,72,62)

data <- data.frame(height, weight)

model <- lm(weight ~ height, data=data)

summary(model)

plot(height, weight,
     main="Height vs Weight",
     xlab="Height",
     ylab="Weight")

abline(model)

---csvcar---
  
car_data <- read.csv("cars.csv")

print(car_data)

cars_buy <- car_data[car_data$BuyPrice >= 3000, ]

print(cars_buy)

sorted_data <- car_data[order(car_data$Buy.Price), ]

print(sorted_data)

group_data <- aggregate(Buy.Price ~ Model, data=car_data, FUN=mean)

print(group_data)
