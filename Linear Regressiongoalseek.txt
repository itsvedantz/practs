---Linear Regression on Iris Dataset (R)---

data(iris)

model <- lm(Petal.Width ~ Petal.Length, data = iris)

summary(model)

plot(iris$Petal.Length, iris$Petal.Width,
     xlab="Petal Length",
     ylab="Petal Width",
     main="Linear Regression")

abline(model, col="blue")

---What-If Analysis using Goal Seek (Excel)---

In Excel enter marks for all components
Create formula for Final Grade (Average)
Data → What-If Analysis → Goal Seek

Fill values:

Field	Value
Set Cell	Final Grade cell
To Value	72
By Changing Cell	Final Exam marks

Excel calculates the required Final Exam marks.